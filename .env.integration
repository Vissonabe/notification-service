# Integration Test Environment Configuration

# Service Configuration
NODE_ENV=test
PORT=3002
API_PREFIX=api/v1

# MongoDB Configuration (will be overridden by memory server)
MONGODB_URI=mongodb://localhost:27017/notification-integration
MONGODB_DB_NAME=notification-integration

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=2

# Authentication
JWT_SECRET=integration-jwt-secret-key-only-for-testing
JWT_EXPIRES_IN=3600
JWT_REFRESH_EXPIRES_IN=86400

# Firebase Configuration (mock for tests)
FIREBASE_PROJECT_ID=integration-project
FIREBASE_CLIENT_EMAIL=integration@example.com
FIREBASE_PRIVATE_KEY=integration-private-key

# APNS Configuration (mock for tests)
APNS_KEY_ID=integration-key-id
APNS_TEAM_ID=integration-team-id
APNS_BUNDLE_ID=integration.bundle.id

# Logging
LOG_LEVEL=error

# Testing
TEST_USER_ID=integration-user-id
TEST_DEVICE_ID=integration-device-id

# Kafka Configuration
KAFKA_BROKERS=localhost:9092
KAFKA_CLIENT_ID=notification-service-integration
KAFKA_GROUP_ID=notification-service-integration-group 