# Test Environment Configuration

# Service Configuration
NODE_ENV=test
PORT=3001
API_PREFIX=api/v1

# MongoDB Configuration (will be overridden by memory server)
MONGODB_URI=mongodb://localhost:27017/notification-test
MONGODB_DB_NAME=notification-test

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=1

# Authentication
JWT_SECRET=test-jwt-secret-key-for-testing-purposes-only
JWT_EXPIRES_IN=3600
JWT_REFRESH_EXPIRES_IN=86400

# Firebase Configuration (mock for tests)
FIREBASE_PROJECT_ID=test-project
FIREBASE_CLIENT_EMAIL=test@example.com
FIREBASE_PRIVATE_KEY=test-private-key

# APNS Configuration (mock for tests)
APNS_KEY_ID=test-key-id
APNS_TEAM_ID=test-team-id
APNS_BUNDLE_ID=test.bundle.id

# Logging
LOG_LEVEL=error

# Testing
TEST_USER_ID=test-user-id
TEST_DEVICE_ID=test-device-id 